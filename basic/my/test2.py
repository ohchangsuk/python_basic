  
    if 0:
        for a in ['1','1.1','a']:
            print(a)
            print(a.isalnum(), a.isalpha(), a.isascii)
            print(a.isdecimal(),a.isdigit,a.isnumeric())
            if a.isnumeric():
                print('정수변환',int(a))
            print('-'*20)

if not tmp: # 공백은 잡는다. 컷
    continue
if not tmp.isnumeric(): # 뭔가는 들고있다
    continue
# 입력값의 정수 변환
tmp=int
if tmp>9 or tmp<1: # 1-9이외의 값이면 컷
if condition:        
    continue
#정상값
Game_level =tmp



#